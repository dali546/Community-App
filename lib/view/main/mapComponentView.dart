import 'package:flutter/material.dart';

class MapComponentView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
      child: new Text("This will be the map page"),
    );
  }
}
