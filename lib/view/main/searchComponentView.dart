import 'package:flutter/material.dart';

class SearchComponentView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
      child: new Text("THis is the Search Page"),
    );
  }
}
